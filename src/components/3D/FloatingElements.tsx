import React from 'react';

export function FloatingElements() {
  // Simplified version without Three.js dependencies
  return <div className="hidden">Floating Elements (disabled for performance)</div>;
}